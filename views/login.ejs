<% include common/header %>
    <div class="container">
        <h1>Entrez votre mot de passe afin de séléctionner un salon :</h1>
        </br>
        </br>
        <form>
            <input type="password" name="password" value="" placeholder="mot de passe" id="input">
            <input type="submit" onClick="checkPassword();" />
        </form>
        </br>
        <script>
            function checkPassword() {            
                var input = document.getElementById("input").value;            
                var full = "/password/" + input;
                $.ajax({
                    url: full,
                    type: 'GET',
                    success: function(result) {
                        console.log("Bravo le veau !");
                        if (result == 200)
                            document.location.href = "http://localhost:8000/salon";
                        else
                            document.location.href = "http://localhost:8000/login";
                    }
                });
            }
        </script>
    </div>
    <% include common/footer %>